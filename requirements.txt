# Pinned for the Docker image. pyproject.toml has the looser ranges
# for `pip install -e .` developer installs.
httpx==0.27.2
pyyaml==6.0.2

# Modbus TCP + RTU (pymodbus speaks both)
pymodbus==3.7.4
pyserial==3.5

# SNMP v2c / v3 — UPS (APC, Eaton, Vertiv), smart PDUs (Raritan, Geist, APC)
pysnmp==7.1.21

# BACnet/IP — Honeywell, Siemens, Johnson Controls BMS + many CRACs
BAC0==2025.6.10

# OPC UA — industrial standard, broadly supported by SCADA stacks
asyncua==1.1.5

# MQTT — subscribe to a customer's existing OT broker
paho-mqtt==2.1.0
